# law/

Canonical Law tree (v2).

- `abi/` machine source-of-truth (registries + schemas + artifact schemas)
- `normative/` human normative texts (contracts, axioms, invariants, boundaries)
- `bindings/` mapping of law to surfaces (CLI/protocol/kernel/vault/control/graph)
- `packs/` compliance + domain packs
