# Graph v1 (Brain Memory Contract)

This note defines the retained Graph v1 contract under historical path `runtime/mind/graph/`.
Primary semantic interpretation is `brain` memory.

## Normative posture

- Graph is a governed cognitive-memory substrate.
- Graph is not sovereign authority.
- Graph cannot bypass `core` governance.
- Graph-derived effects must cross `core` authority and `exec` external-effect gates.

## Layer interpretation

- Semantic layer: canonical memory source for graph meaning.
- Episodic/vector layers: derived support layers.
- Activation layer: runtime-only cognitive assistance layer.
- Authority layer inside graph model: read-only projection, never sovereign source.

## Compatibility note

The path and file names are retained for compatibility with existing traces and references.
Semantics are explicitly aligned to `brain`.
