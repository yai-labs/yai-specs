# Mind (Historical Alias: Brain)

`runtime/mind/` is retained for compatibility of current cognitive runtime artifacts.
Primary normative meaning is `brain`.

## Semantic role

This surface represents the governed cognitive plane:
- cognition and planning surfaces
- memory and retrieval surfaces
- provider-cognitive interaction surfaces

## Constraints

- `brain` is not sovereign authority.
- `brain` must remain governed by `core` policy/enforcement constraints.
- `brain` does not define protocol/platform/support ontology.

## Current modeled scope

`runtime/mind/graph/` remains the active cognitive memory contract surface.
It is interpreted as `brain` memory semantics with retained historical path naming.
