# Kernel (Historical Alias: Core)

`runtime/kernel/` is the retained path for sovereign runtime binding documentation.

Primary normative meaning is `core`.
`kernel` survives here as a migration alias only.

## Semantic role

This surface defines sovereign runtime constraints for:
- authority and governance
- lifecycle control and dispatch
- enforcement of external-effect gates
- traceability-preserving transitions

These are `core` responsibilities.

## Canonical references

- `foundation/boundaries/L1-kernel.md` (rewritten as core boundary with legacy alias)
- `contracts/control/`
- `formal/tla/YAI_KERNEL.tla` (historical artifact name; semantic scope aligned to core sovereignty)

## Conformance rule

Interpretations that treat `kernel` as independent package ontology are non-conforming.
Interpretations that read `kernel` as historical artifact identity for `core` semantics are conforming.
