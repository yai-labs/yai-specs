# Surface Policy Pointer

Defines exposure policy for command surfaces (`surface`, `ancillary`, `plumbing`) and related visibility/stability controls.

## Critical distinction

- Surface policy controls stability and exposure.
- Surface policy does not define runtime ontology.

Runtime ontology remains `core/exec/brain`.
Historical labels in command metadata are compatibility aliases only.
