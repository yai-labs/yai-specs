# Binding — Vault (L0)

Vault binding remains stable and compatibility-sensitive.

## Position

- L0 vault boundary is unchanged.
- Sovereign authority semantics are interpreted via `core` boundaries.
- Historical package labels are not required for ABI interpretation.

## Continuity

`yai_vault_abi.h` and `vault_abi.json` remain canonical and should not be renamed without explicit compatibility migration.
